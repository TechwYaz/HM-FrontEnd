# Base URL of the Laravel backend API (no trailing slash).
VITE_BACKEND_URL=http://127.0.0.1:8000

# Public storage URL for uploaded images.
# Defaults to VITE_BACKEND_URL/storage if not set.
VITE_STORAGE_URL=http://127.0.0.1:8000/storage
